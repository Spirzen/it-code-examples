location = / {
    # только точный запрос к "/"
}

location ^~ /static/ {
    # все URI, начинающиеся с /static/, без проверки регулярок
}

location ~ \.php$ {
    # PHP-файлы
}

location / {
    # fallback
}
