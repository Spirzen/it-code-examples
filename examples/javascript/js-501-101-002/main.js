class <ИмяКласса> {
    constructor(<параметры>) {
        this.<свойство> = <значение>;
    }

    <метод>() {
        // тело метода
    }

    static <статическийМетод>() {
        // тело статического метода
    }

    #<приватноеСвойство> = <значение>;
    #<приватныйМетод>() {
        // тело приватного метода
    }
}
