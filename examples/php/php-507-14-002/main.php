// Блок кода функции
function process() {
    $data = fetch();
    return transform($data);
}

// Блок условия
if ($valid) {
    save();
    notify();
}

// Интерполяция сложного выражения
echo "Total: {$prices[0] * 1.2}";
