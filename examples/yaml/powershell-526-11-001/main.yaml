trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'windows-latest'

steps:
- script: |
    Write-Host "Starting build process..."
    dotnet build
    dotnet test
  
- task: PowerShell@2
  inputs:
    targetType: 'filePath'
    filePath: 'deploy.ps1'
    pwsh: true
