services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      APP_ENV: "dev"
    depends_on:
      - db
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: "example"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
