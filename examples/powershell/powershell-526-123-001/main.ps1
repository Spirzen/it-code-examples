# Объявление и присваивание
$myVariable = "Значение"
$count = 10

# Явное указание типа
[string]$name = "Пример"
[int]$number = 5

# Использование массива
$array = 1, 2, 3, 4, 5
$multiArray = @("A", "B", "C")

# Использование хэш-таблицы
$config = @{
    Server = "localhost"
    Port = 8080
    Enabled = $true
}
